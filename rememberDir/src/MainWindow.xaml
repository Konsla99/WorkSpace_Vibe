<Window x:Class="rememberDir.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:rememberDir"
        mc:Ignorable="d"
        Title="rememberDir" Height="250" Width="400" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#F3F3F3">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="rememberDir" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#333333"/>
        
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Button Content="현재 dir 기록하기" Click="OnSaveClick" Height="45" FontSize="16" Background="#0078D4" Foreground="White" BorderThickness="0" Margin="0,0,0,15">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <Button Content="마지막 dir 위치 열기" Click="OnRestoreClick" Height="45" FontSize="16" Background="#2B88D8" Foreground="White" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="2" Text="준비됨" HorizontalAlignment="Center" Foreground="#666666" FontSize="12"/>
    </Grid>
</Window>
